apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pdf-genie-ingress
  namespace: pdf-genie
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    traefik.ingress.kubernetes.io/router.middlewares: pdf-genie-strip-api@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: pdf-genie.local
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
